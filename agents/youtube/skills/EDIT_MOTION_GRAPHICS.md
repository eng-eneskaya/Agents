# Skill: Edit & Motion Graphics Planning → moved

This skill is now a real Claude Code Skill and lives at:

**`.claude/skills/youtube-edit-plan/SKILL.md`**

It was moved so Claude Code loads it automatically when the work matches, instead of only when someone remembers to open this file. The content is unchanged apart from added `EPISODES.md` steps.

Note the split: `youtube-edit-plan` writes the *plan*; `remotion-motion-graphics` builds and renders the actual asset.

This stub is kept only so older references don't break. Do not edit it — edit the SKILL.md.
